# 서버 포트
PORT=3000

# PC 모니터링 화면(/monitor.html) 접속 비밀번호
ADMIN_PASSWORD=8450

# 태블릿(/sign.html) 접속 토큰
TABLET_TOKEN=1234

# 사용할 태블릿 대수 (1 ~ 이 숫자까지 id 사용)
TABLET_COUNT=10

# 같은 코드를 v1/v2/v3처럼 여러 개 독립적으로 배포할 때, 화면에 표시할 이름
# (비워두면 아무것도 표시하지 않음)
INSTANCE_NAME=V1
